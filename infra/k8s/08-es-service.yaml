apiVersion: v1
kind: Service
metadata:
  name: logcollector-es # application.yml의 기본값과 일치시킴
spec:
  selector:
    app: elasticsearch
  ports:
    - protocol: TCP
      port: 9200
      targetPort: 9200